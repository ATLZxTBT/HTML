### Criar app.py /// Criar diretório: templates /// criar os próximos arquivos dentro do diretório


from flask import Flask, render_template

app = Flask(__name__)
@app.route("/")
def pagina():
    text_html = "essa mensagem é do python"
    lista_exemplo = []
    return render_template(("Ilum.html"))
